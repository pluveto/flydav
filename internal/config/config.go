// internal/config/config.go
package config

import (
    "gopkg.in/yaml.v2"
    "io/ioutil"
)

type Config struct {
    // 定义配置结构
}

func Load(path string) (*Config, error) {
    // 从文件加载配置
    // 解析YAML
    // 返回配置结构体实例
    // ...
}
